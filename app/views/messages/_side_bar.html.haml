.SideBar
  .LeftHeader
    .LeftHeader__username
      %a.LeftHeader__name
        = current_user.name
      .LeftHeader__icon
        %i.list
          = link_to new_group_path do
            = icon('fa', 'edit', class: 'far.fa-edit')
        %i.list
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class: 'fa.fa-cog')
  .GroupList
    - current_user.groups.each do |group|
      .Group
        = link_to do
          .Group__name
            = group.name
          .Group__message
            メッセージはまだありません